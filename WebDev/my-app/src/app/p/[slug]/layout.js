export const metadata = {
    title : 'PDP Page',
} // it is like head tag for whole page


function PDPLayout({children}) {
    return <>
    {children}
    </>
}

function PDPHeader({children}) {
    return <div style
}